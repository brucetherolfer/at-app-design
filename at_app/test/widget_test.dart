import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('AT App smoke test placeholder', (WidgetTester tester) async {
    // Background timer and Isar require physical device / full init.
    // Add integration tests here once running on device.
    expect(true, isTrue);
  });
}
